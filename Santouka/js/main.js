"use strict";
{
  // All, [elem, elem, elem, elem]
  // selecotr, elem

//   const bigTextNodeList = document.querySelectorAll("li");
//   console.log("bigTextNodeList",bigTextNodeList);
//   console.log("bigTextList[0]",bigTextNodeList[0]);

//   // ".js-menu-li",
//   const bigTextListArr = Array.from(bigTextNodeList);
//  bigTextListArr.map((big) => {
//    big.addEventListener("mouseover", () => {
//      big.classList.add("big");
//     });
//    big.addEventListener("mouseleave", () => {
//      big.classList.remove("big");
//     });
//   });




}
